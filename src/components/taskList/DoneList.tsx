import * as React from 'react';
import VDoneList from './vacs/VDoneList';

interface DoneListProps {

}

const DoneList = ({}: DoneListProps) => {
  return (
		<VDoneList/>
  );
};

export default DoneList;
